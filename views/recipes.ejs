<%- include('nav.ejs') %>


    <body>
        <p class="hiddenData"><%= is21 %></p>
        <p><%= is21 %></p>
        <h1 class="recipeHeader">Recipes</h1>
       
        <div class="recipes">
            <% recipes.forEach(function(recipe) { %>
                <div class="singleRecipe<% if (is21 == false){if(recipe.alcoholic === 'alcoholic') { %> blurRecipe<% }} %>">

                    <% if (recipe.filepath){ %>
                        <img src="<%= recipe.filepath %>" alt="Uploaded Image">
                        <% } %>

                            <div class="recipeListInfo">

                                <a href="/recipes/<%= recipe._id %>?is21=<%= is21 %>">
                                    <h2>
                                        <%= recipe.name %>
                                    </h2>
                                </a>
                                <p>
                                    <%= recipe.description %>
                                </p>
                                <p>
                            <%= recipe.category %>
                        </p>
                               <% if (recipe.category=="Drinks" ) {%>
                            <p>
                                
                            </p>
                            <% } %>
                            </div>
                </div>
                <% }); %>

        </div>
        <script>
            let ofAgeCheck =  <%= is21 %>
            console.log(ofAgeCheck);
            
        </script>
    </body>


    <%- include('footer.ejs') %>